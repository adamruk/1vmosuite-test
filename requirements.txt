# 1vmo Suite — Python 3.13 on Windows with RTX 4080 NVENC
# PySide6: pinned to 6.9.3 — the final patch release of the 6.9.x series
# (no further 6.9.x patches expected). 6.9.1 was originally proposed in
# PHASE_2C_PLAN.md as the stability target, but Qt issue PYSIDE-3115
# (https://bugreports.qt.io/browse/PYSIDE-3115) causes a sporadic shiboken
# segfault on shutdown under 6.9.1 that does not reproduce on 6.9.3.
# 6.9.3 keeps the same Qt 6.9 ABI guarantees while picking up that fix,
# so the team-internal pin moved forward by two patch releases.
PySide6==6.9.3
requests>=2.31
# nvidia-ml-py 12.x+ for NVENC hardware detection (gpu_detect.py).
# Safe to remove on non-NVIDIA systems — gpu_detect falls back gracefully.
# Note: the package is 'nvidia-ml-py' on PyPI but imports as 'pynvml'.
# v13.x removed nvmlInit_v2; our code uses bare nvmlInit() which is correct
# for both v12 and v13+. See FFMPEG_CPU_TO_NVENC_REFERENCE.md §9.
nvidia-ml-py>=12.0
Pillow>=10.0
pydantic>=2.0,<3
platformdirs>=4.0,<5
pytest>=8.0,<9
yt-dlp>=2025.06.09
